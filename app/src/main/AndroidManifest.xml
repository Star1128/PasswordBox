<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.ethan.passwordbox">

    <application
            android:name="com.ethan.passwordbox.config.MainApplication"
            android:allowBackup="true"
            android:icon="@mipmap/box"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/box"
            android:supportsRtl="true"
            android:theme="@style/Theme.PasswordBox">
        <activity android:name="com.ethan.passwordbox.ui.AddActivity" />
        <activity android:name="com.ethan.passwordbox.ui.MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>